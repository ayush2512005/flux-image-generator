# FLUX Image Generator

This project runs the FLUX.1-schnell model locally using Hugging Face Diffusers.

## Features
- Text-to-image generation
- Uses FLUX diffusion transformer model
- Runs locally with PyTorch

## Installation

```bash
pip install -r requirements.txt
